#
# Right half is the split peripheral. Keep the OLED enabled there, but disable
# central-only widgets to avoid noisy Kconfig warnings and inconsistent UI.
#

CONFIG_ZMK_USB=n
CONFIG_ZMK_WIDGET_OUTPUT_STATUS=n
CONFIG_ZMK_WIDGET_LAYER_STATUS=n
CONFIG_ZMK_WIDGET_WPM_STATUS=n
